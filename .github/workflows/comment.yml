name: comment example
on: push
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: Create comment
        uses: ./
        with:
          type: create
          body: "- [ ] Run tests"
          issue-number: '1'
          token: ${{ secrets.GITHUB_TOKEN }}
